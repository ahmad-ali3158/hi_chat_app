import 'package:flutter/material.dart';

class AboutCaller extends StatelessWidget {
  const AboutCaller({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
