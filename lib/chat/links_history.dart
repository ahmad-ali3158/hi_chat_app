import 'package:flutter/material.dart';

class LinksHistoyr extends StatelessWidget {
  const LinksHistoyr({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
